<% add_decidim_page_title(t("departments.title", scope: "decidim.admin")) %>
<div class="card">
  <div class="item_show__header">
    <h1 class="item_show__header-title">
      <%= t("departments.title", scope: "decidim.admin") %>
      <% if allowed_to? :update, :organization %>
        <%= link_to t("actions.add", scope: "decidim.admin"), new_department_path, class: "button button__sm button__secondary" %>
      <% end %>
    </h1>
  </div>

  <div class="table-scroll">
    <table class="table-list">
      <thead>
      <tr>
        <th><%= t(".headers.name") %></th>
        <th><%= t(".headers.actions") %></th>
      </tr>
      </thead>

      <tbody>
        <%= render partial: "department", collection: @items.order('name ASC') %>
      </tbody>
    </table>
  </div>
</div>
