<% add_decidim_page_title(t(".title")) %>
<%#= admin_tabs(:admin_study_notes_menu).render %>

<div class="card">
  <div class="item_show__header">
    <h1 class="item_show__header-title">
      <%= t(".title") %>
      <%= link_to 'Uwagi do planu ogólnego', study_notes_path, class: "button button__sm button__secondary" %>
      <%= link_to 'Dodaj podkład mapowy', new_map_background_path, class: "button button__sm button__secondary" %>
    </h1>
  </div>

  <div class="card-section">
    <div class="table-scroll">
      <table class="table-list">
        <thead>
        <th>Nazwa</th>
        <th>Plik</th>
        <th>Typ podkładu</th>
        <th>Data dodania</th>
        <th class="actions">Akcje</th>
        </thead>
        <tbody>
        <% @map_backgrounds.each do |item| %>
          <tr data-id="<%= item.id %>">
            <td>
              <%= item.name %>
            </td>
            <td>
              <%= link_to item.file.filename, main_app.url_for(item.file) if item.file.attached? %>
            </td>
            <td>
              <%= item.file_type && item.file_type == 'raster' ? 'Rastrowy' : 'Wektorowy'  %>
            </td>
            <td>
              <%= l item.created_at, format: "%d/%m/%Y - %H:%M" %>
            </td>
            <td class="table-list__actions">
              <%= icon_link_to "pencil-line", [:edit, item], t("actions.edit", scope: "decidim.admin"), class: "action-icon--edit", method: :get, data: {} %>
              <%= icon_link_to "delete-bin-line", map_background_path(item), t("actions.destroy", scope: "decidim.admin"), class: "action-icon--remove", method: :delete, data: { confirm: t("actions.confirm_destroy", scope: "decidim.admin") } %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
      <%= paginate @map_backgrounds, theme: "decidim" %>
    </div>
  </div>
</div>