<%= decidim_form_for(@form, url: map_background_path(@form), html: { autocomplete: 'off', class: 'form' }) do |f| %>
  <div class="card">
    <div class="card-divider">
      <h2 class="card-title"><%= t ".title" %></h2>
    </div>
    <div class="card-section">
      <%= render partial: "form", object: f %>
    </div>
  </div>
  <div class="button--double form-general-submit">
    <%= link_to 'anuluj', map_backgrounds_path, class: 'button hollow' %>
    <%= f.submit t(".update") %>
  </div>
<% end %>
